# 验证记录 · 2026-09-12

## README 特别鸣谢

- 按作者要求在中英文 README 添加电丸科技 AK 的视频灵感鸣谢，链接到 YouTube 与哔哩哔哩主页，不涉及其他产品比较。
- 人工文档回归：两份 README 的署名、致谢范围和平台链接一致；链接核对其公开频道介绍所列地址，哔哩哔哩链接移除跟踪参数。YouTube 目标返回频道页；哔哩哔哩空间直接抓取返回 412，已用频道介绍中的 UID 交叉核对，未将其记作可用性实测通过。

## 首次公开源码准备

- README 改为安装、权限与操作说明；开发细节移到 docs。新增英文介绍、MIT、贡献指南、兼容性/问题模板和 macOS CI。未宣称已提供公证 App 下载。
- 38 项 Swift 测试通过，0 失败、0 跳过；15 项发行工具测试通过，覆盖签名身份限制、公证拒绝、staple/Gatekeeper 失败、版本路径、锁和禁止覆盖已有发行目录。
- 开发模式实际构建、稳定签名跨内容验证和打包 Metal 自检通过；App 包含 MIT 文本。修正系统 Bash 在 nounset 下不能展开空数组的问题。
- 本机无 Developer ID Application，实测发行入口在构建前拒绝，开发 App 校验和不变、没有生成发行资产。已准备硬化运行时、时间戳、公证、票据装订、Gatekeeper、ZIP 与校验和流程；真实 Apple 公证及干净机器安装尚未执行。
- 两位独立审查覆盖公开文档和发行流程。修复并发构建可能替换发行快照的问题：打包使用公共锁，发行直接构建到独占工作目录，新增回归并复查通过。
- 首轮 GitHub CI 中，发行工具和普通逻辑通过，Metal 读回为全零而失败。增加不依赖 LidFold shader 的纯清屏/读回探针；仅在 GitHub 托管环境连此探针也失败时报告 GPU 不可用并显式跳过。物理设备及实际 shader 像素差异仍按失败处理，不将托管 CI 当作 GPU 实机验收。

## README 移除开发期授权迁移步骤

- 核对当前签名脚本、权限逻辑与 Git 记录：构建要求有效证书签名，权限逻辑没有旧版迁移依赖，仓库无远程和发布标签。旧版临时签名迁移属于开发期排障记录，不作为新用户安装步骤。
- 从 README 移除旧版迁移段落及授权重置命令；首次授权与重新打开应用的通用指引保留，本文件中的历史验证记录保留。
- 文档回归检查：README 不再出现旧版本号或授权重置命令；首次授权、权限管理入口和签名要求仍存在；Markdown 代码块配对、脚本路径与 `git diff --check` 均通过。未执行权限重置或修改应用代码。

## README 公开介绍整理

- 移除其他产品名称、站点链接和来源比较文案；开头仅介绍 LidFold 的行为与本地处理方式，不使用免费替代、价格比较或商业定位比较。
- 区分最低部署目标与已实测环境，补齐「使用方式」标题，避免使用步骤归入历史授权排障。
- 文档回归用例：检查 README 不含外部产品链接或比较用语；运行脚本路径全部存在；保留授权、签名、机型兼容性和能耗边界。检查通过，`git diff --check` 通过。本次仅修改文档，不涉及应用代码。

## 0.2.3 说明设置窗口不参与折叠

- 通用页运行说明、效果页外观说明明确写出：LidFold 的设置窗口不参与折叠，方便随时调整；其他应用随桌面一起变化。
- 更新界面回归用例：分别打开通用与效果页，确认说明完整出现在辅助功能树中；效果页截图确认文字自然换行、未截断，滑块和预览入口可见。实际验收通过。
- 38 项自动测试通过，0 失败、0 跳过；release 构建、签名完整性、Info.plist 和打包 Metal 自检通过。本次仅改说明文字，不改变窗口或折叠行为。

## 0.2.2 移除侧栏折叠按钮

- 在侧栏列上移除系统生成的 `sidebarToggle`，列可见性固定为 `.all`；保留原生列表、页面标题和内容滚动。
- 新增界面回归用例：启动通用页，确认侧栏三个入口可见、辅助功能树不存在 `Hide Sidebar` 按钮；点击效果页，确认标题更新、三个滑块和预览入口仍可访问，侧栏保持显示。两项均已在打包应用中实际通过，截图确认标题栏按钮消失。
- 38 项自动测试通过，0 失败、0 跳过；release 构建、签名完整性、Info.plist 与打包 Metal `--self-check` 通过。

## 0.2.1 设置打开时真实开合无效果

- 根因：0.2.0 的 `scheduleRendering()` 和 `renderTick()` 均用 `!settings.settingsVisible` 阻止渲染。即使已经启用、收到角度和桌面帧，只要设置未关闭就没有真实开合效果。现场界面显示「已启用，设置期间隐藏效果」，符合该路径；127° 的瞬时读数本身高于 105° 阈值，并不构成低角度渲染证据。
- 修复：设置可见性只决定窗口层级，不再决定能否渲染。折叠期间设置位于桌面效果上方，保持可操作；效果隐藏后恢复普通窗口层级。设置仍被排除在桌面捕获之外。权限弹窗位于二者上方。
- 通用与效果页说明开始折叠的角度阈值、设置保持清晰且桌面实时响应，消除开关与可见行为的矛盾。
- 38 项测试通过，0 失败、0 跳过，包含真实 Metal GPU 测试与 3 项新展示策略回归。release 构建、Info.plist、稳定签名跨内容回归和打包 `--self-check` 均通过。
- 实际运行：0.2.1 自动启用，收到 115° 传感器数据和桌面帧，无需再次授权；用户原有「启动时启用效果」偏好保留。
- **用户物理验收通过**：要求保持设置打开，将屏幕合至约 90° 后展开到 115° 以上；用户确认「已恢复正常，开合有变化」。这是本次真实联动恢复的验收依据。
- 两位独立 Agent 对渲染入口、窗口层级、预览、暂停与捕获中断路径完成只读对抗性审查，均未发现确定缺陷。未增加新的真实睡眠、登录或续航验收。

## 0.2.0 原生设置与基础功能

- 35 项 Swift 测试全部通过，0 失败、0 跳过，包括真实 Metal GPU 测试。新增偏好持久化与容错、登录项系统状态、睡眠/锁屏等多重中断、独立预览截止计时器测试。
- release 构建、代码签名与 Info.plist 校验通过；跨内容重签名身份回归和打包应用 `--self-check` 均通过，沿用原证书签名，无需重置屏幕录制权限。
- 实际 GUI：原生侧栏、分组 Form、开关、滚动布局和效果滑块均可见。三个滑块提供独立辅助功能名称、数值和增减动作；实际通过增量动作将折叠角度从 105 调整至 110.5。
- 实际登录项注册与注销：在界面开启「登录时启动」后系统状态返回已启用，关闭后回到未启用；交付时保持默认关闭。首次注册前系统可能返回 `notFound`，实测仍可成功注册，因此不将此状态误报为固定安装错误，并加入回归测试。
- 实际重启：临时开启「启动时启用效果」，通过应用内入口重新打开；效果自动启用，显示真实 125°，没有再次请求屏幕录制权限；折叠角度仍为 110.5，证明跨进程保存。验收后关闭自动启用，并恢复效果默认值。
- 实际预览：收到桌面帧后预览按钮可用；点击后设置隐藏，预览结束返回效果页，默认参数为 105°、模糊 14、阴影 0.55。
- 两位独立 Agent 完成对抗性审查和复查。修复最小化设置后效果被持续抑制的问题，以及捕获短暂中断导致预览无法返回设置的问题；后者新增 3 项回归测试。
- 验证边界：桌面工具读取窗口会重新显示窗口，最小化后的持续显示效果未获得独立 GUI 证据；最小化/还原回调已经代码复查。真实注销/登录启动、睡眠/唤醒、锁屏/解锁、物理开合连贯性与全局物理快捷键仍需人工验收，未以策略单元测试替代系统通知链验收。未进行续航或 HDR 验收。

## 0.1.1 授权循环修复

- 系统 `tccd` 日志确认根因：`Failed to match existing code requirement for subject local.leon.LidFold and service kTCCServiceScreenCapture`。旧授权绑定 `b2befe4a…`，0.1.0 最终构建为 `39b29a8c…`，临时签名身份失配。
- 0.1.1 改用已有 Apple Development 证书；指定身份要求包含 Apple 证书链、应用标识与证书身份，不再绑定某次构建的 `cdhash`。
- 16 项 Swift 测试通过，0 失败、0 跳过，包含 5 项新权限流程测试、2 项重启资源交接测试。
- 跨构建签名回归通过：修改资源、重新签名后的副本具有不同 CDHash，但满足原 app 的指定身份要求；两者的指定身份要求完全一致。
- 生产 app 的代码签名完整性、Info.plist、包内 shader 与真实 Metal 初始化通过。
- 已通过 `tccutil reset ScreenCapture local.leon.LidFold` 清理旧签名绑定，用户为修复版完成一次授权。
- 实际 GUI：点击启用后直接显示 125°，状态为「已启用」，预览按钮可用，证明已读取传感器并收到桌面帧。
- 实际重启回归：通过「授权帮助… → 重新打开应用」退出重启，再次点启用仍直接显示 125° 和可用预览，没有再跳转授权。
- 实际跨构建回归：加入重启交接修复、重新编译并用同一证书签名后，再次启用直接显示 127° 和可用预览，未再次授权。
- 构建保护回归：应用运行时脚本拒绝替换，最终可执行文件 SHA-256 保持不变；显式指定临时签名 `-` 也被拒绝。
- 两位独立 Agent 已复查本轮权限与签名修改；重启时提前释放热键、失败恢复的交接修复也已复查通过。物理键盘快捷键仍保留为人工验收项。

以下为 0.1.0 初次验证历史，其中临时签名已由 0.1.1 替换。

环境：MacBook Pro / Apple M4 / Mac16,1，macOS 26.6.2，Swift 6.3.3。

## 已完成

- 非沙箱实读 HID 传感器：设备打开成功，report 1 返回 `[1, 122, 0]`，对应 122°。
- `bash scripts/test.sh`：9 项测试通过，0 失败、0 跳过。包含真实 Metal GPU 测试；另一次沙箱运行因无法访问 Metal 跳过 GPU 测试，该次不计入 GPU 验收。
- `bash scripts/build-app.sh`：release 编译、本机 ad-hoc 签名、`codesign --verify --deep --strict` 与 Info.plist 验证通过。
- 最终 app 执行 `--self-check`：包内 shader 资源与真实 Metal pipeline 初始化通过。
- GUI：打包应用实际启动，窗口布局与按钮正常；用户授权后初次构建显示实时 122°，收到桌面帧后预览按钮可用。
- 两位独立 Agent 完成对抗性审查并复查：修复设置被效果覆盖、菜单追踪期间 watchdog 暂停，以及构建资源目录问题；补充锁屏通知和首帧超时保护。

## 需要实际操作确认

- 最终构建重签名后的屏幕录制授权刷新。
- 实际连续开合时的感知延迟、平滑程度与开盖恢复。
- 预览期间没有递归捕获；菜单设置可见；⌘⇧Esc 全局暂停。
- 真正睡眠/唤醒、锁屏/解锁及外接显示器变化后的行为。

上述人工项目不能用 GPU 单元测试或读取一次传感器替代。未进行续航测试、HDR 匹配或不同 Mac 型号兼容性验收。
